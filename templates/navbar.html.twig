{% if app.user is not null %}
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <a class="navbar-brand" href="#">Bonjout {{ app.user.firstname }} - {{ app.user.lastname }}</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
                <li class="nav-item active">
                    <a class="nav-link" href="{{ path('app_employee_index') }}">Les employés</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_logout') }}">Me déconnecter</a>
                </li>

                {% if app.user.hasRole("ROLE_DIRECTION") %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_employee_new') }}">Ajouter un employé</a>
                </li>
                {% endif %}

            </ul>
        </div>
    </nav>
{% endif %}